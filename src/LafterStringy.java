import java.util.Arrays;

public class LafterStringy {
public static void main(String[] args) {
	/*
	 * #include <iostream>
#include <cctype>
#include <cstring>

using namespace std;
void sort(string str[],int length[],int n){
	for(int i=0;i<n-2;i++){
		for(int j=0;j<n-i-1;j++){
			if(length[j]>length[j+1]){
				int t=length[j];
		}
		}
	}
}
void check(string s){
string str[50];
	int length[50];
	int k=0;
	string word="";
	for(int i=0;i<s.length();i++){
		if(isspace(s[i])){
		str[k]=word;
			length[k++]=word.length();
			word="";
		}
		else
		 word+=s[i];
	}
	str[k]=word;
	length[k++]=word.length();
	sort(str,length,k);
	for(int i=0;i<k;i++)
	  cout<<str[i]<<" ";
}
int main() {
	string n;
	getline(cin,n);
	string s="";
	for(int i=0;i<stoi(n);i++){
	   getline(cin,s);
	   check(s);
	   cout<<"\n";
	}
	return 0;
}

*/
  }
}
